# 🧭 SM Codex Portal — L1 → L2 Escalation Learning Platform

**SM Codex Portal** — это обучающий портал для сотрудников службы поддержки, продаж, внедрения и операционного отдела, 
разработанный для внутренних программ повышения квалификации и перехода с уровня **L1 → L2**.  

Проект построен на **Vanilla JavaScript (ES Modules)** и полностью функционирует **без серверной части** — 
всё работает локально, прямо из браузера.

---

## 🚀 Основные возможности

- 🎓 **Интерактивные квизы** с динамической загрузкой вопросов из JSON  
- 💾 **История прохождений** квизов с процентом успеха и датой (через LocalStorage)  
- 📊 **Страница “Мои результаты”** с таблицей всех квизов  
- 🌗 **Тёмная/светлая тема**, включая поддержку системной темы  
- ⚙️ **SPA-навигация** без перезагрузки страниц (через `hashchange`)  
- 🔄 **Автоматическая инициализация страниц** по `data-page`  
- 💡 **Модульная архитектура**, легко расширяемая и поддерживаемая  

---

## 🗂️ Структура проекта

```
SM-Codex-Portal-Updated/
├── index.html                      # Главная страница и точка входа
├── README.md                       # Документация
├── data/
│   └── quizzes/                    # Банки квизов (JSON)
│       ├── support.json
│       ├── sales.json
│       ├── operations.json
│       └── implementation.json
├── css/
│   ├── base.css                    # Общие стили (шрифты, переменные)
│   ├── layout.css                  # Глобальная сетка и компоновка
│   ├── components.quiz.css         # Стили модалки квизов и кнопок
│   └── sections/
│       ├── themes.css              # Тёмная / светлая тема
│       ├── animations.css          # Переходы и плавные эффекты
│       └── results.css             # Страница “Мои результаты”
└── js/
    ├── main.js                     # Главная точка инициализации приложения
    ├── config.js                   # Конфигурация путей и констант
    ├── core/                       # Базовые системные модули
    │   ├── router.js               # Маршрутизация и анимация переходов
    │   ├── theme.js                # Управление темами
    │   └── utils.js                # Автоинициализация страниц
    └── modules/                    # Модули приложения
        ├── quiz/                   # Механика квизов
        │   ├── registry.js         # Загрузка и кеширование JSON-квизов
        │   └── engine.js           # Интерфейс, ответы, подсчёт баллов
        ├── results/                # Модуль страницы "Мои результаты"
        │   └── pages/
        │       └── main.js
        ├── support/
        │   └── pages/main.js       # Страница поддержки
        ├── sales/
        │   └── pages/main.js       # Страница продаж
        ├── operations/
        │   └── pages/main.js       # Страница операций
        └── implementation/
            └── pages/main.js       # Страница внедрения
```

---

## ⚙️ Логика работы приложения

### 1. Точка входа (`main.js`)
- При загрузке страницы вызываются:
  ```js
  Theme.init();
  Router.init();
  Utils.autoInitPage(section);
  ```
- После этого:
  - Подключается текущая тема;
  - Отрисовывается нужный раздел по `hash`;
  - Инициализируется соответствующий модуль страницы.

### 2. Маршрутизация (`core/router.js`)
- Работает через изменение `location.hash` (`#support`, `#sales`, `#results` и т.д.);
- Обеспечивает плавный fade-переход между разделами;
- Обновляет активный пункт меню и вызывает инициализацию модуля.

### 3. Темизация (`core/theme.js`)
- Автоматически определяет системную тему (`prefers-color-scheme`);
- Сохраняет пользовательский выбор в `localStorage`;
- Применяет CSS-классы `.light-theme` / `.dark-theme`.

### 4. Квизы (`modules/quiz/`)
- Данные хранятся в `data/quizzes/*.json`.
- `registry.js` загружает JSON и кэширует данные.
- `engine.js`:
  - отображает вопросы в модальном окне;
  - подсвечивает правильные/неправильные ответы;
  - подсчитывает баллы;
  - сохраняет результаты в LocalStorage;
  - отображает финальный экран с кнопкой “📊 Мои результаты”.

### 5. Страница “📊 Мои результаты” (`modules/results/pages/main.js`)
- Загружает данные из LocalStorage;
- Отображает таблицу со всеми квизами, процентами и датами;
- Позволяет очистить историю.

---

## 💾 Хранение данных (LocalStorage)

Результаты квизов сохраняются в формате:

```json
{
  "support": {
    "l1-intro": {
      "score": 4,
      "total": 5,
      "percent": 80,
      "date": "18.11.2025, 12:42"
    }
  }
}
```

Ключ:
```js
QuizEngine.STORAGE_KEY = 'smcodex_quiz_results_v1';
```

---

## 🧠 Правила использования и расширения

### ➕ Добавление нового квиза
1. Создай JSON-файл в `data/quizzes/`.
2. Добавь уникальный `quiz-id`.
3. В HTML добавь кнопку:
   ```html
   <button data-quiz="quiz-id">Пройти квиз</button>
   ```
4. Инициализируй через `QuizEngine.init(container, 'section')`.

### ➕ Добавление нового раздела
1. Создай `js/modules/hr/pages/main.js`.
2. Добавь пункт в навигацию `index.html`.
3. Создай `data/quizzes/hr.json`.

---

## ⚙️ Запуск проекта

1. Скачай или клонируй проект:
   ```bash
   git clone https://github.com/<your-org>/sm-codex-portal.git
   cd sm-codex-portal
   ```
2. Открой `index.html` в браузере.
3. Проект полностью автономен — сервер не требуется.

---

## 🧾 Авторы и назначение

**Команда SM Codex / Support Training**  
Проект разработан для внутренних обучающих программ компании.  

Разработано с ❤️ на чистом JavaScript.  
© 2025 · SM Codex Team
