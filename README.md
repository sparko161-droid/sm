# 🧭 SM Codex — L1 → L2 Escalation Training Portal

**SM Codex** — это внутренний обучающий веб-портал для сотрудников поддержки, продаж и внедрения.  
Он помогает переходить с уровня **L1 → L2**, проходить квизы, использовать калькуляторы и изучать шаблоны взаимодействия с клиентами.  
Проект реализован на **чистом JavaScript (Vanilla JS)** без фреймворков и серверной логики.

---

## 🚀 Основные возможности

- 🎓 Интерактивные **квизы** с вопросами по направлениям: Support, Sales, Operations, Implementation  
- 💰 **Калькуляторы** уровней L1 и L2 с учётом налоговой ставки  
- 🌗 Переключение **тёмной / светлой темы** с сохранением предпочтений  
- 🧮 Таблицы и чек-листы для **оценки испытательного срока**  
- 🧭 Простая **навигация без перезагрузки страниц** (SPA через `hashchange`)  
- ⚙️ Полностью **модульная архитектура**, легко расширяемая  

---

## 🗂️ Архитектура проекта

```
sm-codex-update-l1-to-l2-escalation-content/
├── index.html
├── README.md
├── css/
│   ├── base.css                 # Общие переменные, шрифты и цвета
│   ├── layout.css               # Сетка и структура макета
│   ├── components.*.css         # Стили отдельных UI-компонентов
│   └── sections.support.css     # Стили раздела поддержки
└── js/
    ├── core/
    │   ├── router.js            # SPA-маршрутизация через hash
    │   └── theme.js             # Переключение и сохранение темы
    ├── calculators/
    │   ├── l1.js                # Калькулятор для уровня L1
    │   └── l2.js                # Калькулятор для уровня L2
    ├── quizzes/
    │   ├── quiz-engine.js       # Логика квизов (контроллер)
    │   ├── quiz-registry.js     # Реестр квизов (модель)
    │   ├── bank.*.js            # Банки квизов по направлениям
    │   └── init.js              # Инициализация квизов по секциям
    ├── support/                 # Модули раздела поддержки
    │   ├── support-filters.js
    │   ├── support-accordions.js
    │   ├── support-templates.js
    │   ├── support-probation.js
    │   ├── probation-table.js
    │   ├── support-page.js
    │   ├── support-onboarding-page.js
    │   └── support-matrix-page.js
```

---

## ⚙️ Техническое описание

### **Frontend**
- Чистый HTML + CSS + Vanilla JS  
- Без зависимостей (не требует npm, webpack и пр.)
- Модульность достигается через глобальные пространства `window.*`

### **Архитектурные паттерны**
- **MVC-подобная структура**:  
  - Model — `quiz-registry.js`, `bank.*.js`  
  - Controller — `quiz-engine.js`, `support-page.js`  
  - View — HTML + CSS  
- **SPA-навигация** через `core/router.js`
- **Темизация** и UX-персонализация через `core/theme.js`

---

## 🧩 Основные модули

| Модуль | Назначение |
|--------|-------------|
| `router.js` | Управляет переходами между разделами через `#hash` |
| `theme.js` | Меняет и сохраняет тему (тёмная / светлая) |
| `quiz-engine.js` | Обработка квизов: показ, ответы, подсчёт |
| `quiz-registry.js` | Реестр всех квизов, API для доступа к ним |
| `l1.js`, `l2.js` | Калькуляторы зарплаты и показателей эффективности |
| `support-filters.js` | Фильтрация блоков по линии поддержки |
| `support-accordions.js` | Разворачиваемые аккордеоны |
| `probation-table.js` | Чеклист испытательного срока с автосохранением |
| `support-templates.js` | Система готовых шаблонов сообщений |
| `init.js` | Инициализация квизов в нужной секции |

---

## 🎨 Визуальная часть (CSS)

- Используются **CSS-переменные** для цвета, шрифта и адаптации тем.  
- Модульная структура: каждый компонент имеет собственный CSS-файл.  
- Поддержка адаптивности через `grid` и `flex`.  
- Лёгкая интеграция тёмной темы (`body.dark-theme`).

---

## 🧠 Логика SPA

1. При загрузке `index.html`:
   - `theme.js` применяет сохранённую тему;
   - `router.js` активирует раздел по хэшу (`#support`, `#calc`, и т.д.);
   - `quiz-registry.js` регистрирует банки квизов;
   - `quiz-engine.js` связывает интерфейс и данные;
   - `support-*` модули активируются для конкретных страниц.

2. При смене хэша меняется значение `data-section` у `#content`,  
   что позволяет CSS и JS отрисовывать нужный раздел без перезагрузки.

---

## 🧭 Запуск проекта

1. Склонируйте репозиторий:
   ```bash
   git clone https://github.com/<your-org>/sm-codex-escalation-portal.git
   cd sm-codex-escalation-portal
   ```

2. Откройте файл:
   ```
   index.html
   ```
   💡 Проект работает полностью **локально**, интернет не требуется.

3. Для публикации можно использовать любой статический хостинг (например, GitHub Pages или Netlify).

---

## 🧰 Рекомендации по улучшению

| Направление | Предложение |
|--------------|--------------|
| **Архитектура JS** | Перейти на ES-модули (`import/export`), отказаться от `window.*` |
| **Контент квизов** | Хранить вопросы в JSON и загружать через `fetch()` |
| **Тема** | Добавить поддержку системной темы (`prefers-color-scheme`) |
| **Анимации** | Ввести плавные переходы при переключении секций |
| **i18n** | Поддержка нескольких языков |
| **Сборка** | Добавить сборщик (например, Vite) для минификации и оптимизации |

---

## 🧾 Авторы и назначение

**Команда “Стандарт Мастер” (Support Training)**  
Проект создан для внутренних обучающих программ сотрудников  
и используется для развития компетенций с L1 до L2.

---

© 2025 · SM Codex — Escalation Learning Portal  
Разработано с любовью ❤️ на чистом JavaScript.
