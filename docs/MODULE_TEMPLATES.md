# MODULE_TEMPLATES.md
Шаблоны модулей и контента для SM Codex Portal

Документ содержит стандартизированные шаблоны, которые должны использоваться при создании:
- новых разделов,
- калькуляторов,
- квизов,
- кейсов,
- онбордингов,
- матриц,
- runbook-документов.

Единый шаблон гарантирует:
- предсказуемость структуры,
- удобство поддержки,
- чистоту архитектуры,
- единообразный стиль.

---

# 1. Шаблон модуля раздела (SPA-модуль)

```js
// /js/modules/<name>/main.js

export default {
  name: "<name>",

  async init(container) {
    container.innerHTML = `
      <section class="section-block" data-<name>-section="overview">
        <h2>Название раздела</h2>
        <p class="lead">Краткое описание.</p>
      </section>

      <!-- Добавьте другие секции -->
    `;

    this.bindEvents(container);
  },

  bindEvents(container) {
    // Навешиваем обработчики
  },

  destroy() {
    // Снимаем обработчики, очищаем модификации DOM
  }
};
````

---

# 2. Шаблон калькулятора

```js
// /js/calculators/<calc>.js

import { formatMoney, parseNumber } from "./core.js";

export function init<CalcName>Calculator(root) {
  const fieldExample = root.querySelector("#example");
  const resultEl = root.querySelector("#result");

  function recalc() {
    const value = parseNumber(fieldExample.value, 0);
    const result = value * 2; // пример формулы
    resultEl.textContent = formatMoney(result);
  }

  fieldExample.addEventListener("input", recalc);
  recalc();
}
```

---

# 3. Шаблон HTML-калькулятора (templates.js)

```js
export const MY_CALC_TEMPLATE = `
  <div class="calc-card">
    <h3>Название калькулятора</h3>

    <label>
      Входное поле:
      <input id="example" type="number" />
    </label>

    <div class="calc-result">
      Итог: <span id="result"></span>
    </div>
  </div>
`;
```

---

# 4. Шаблон квиза (JSON)

```json
{
  "id": "quiz-id",
  "category": "support",
  "title": "Название квиза",
  "questions": [
    {
      "prompt": "Текст вопроса",
      "options": [
        { "text": "Вариант 1", "correct": false, "note": "Комментарий" },
        { "text": "Вариант 2", "correct": true, "note": "Почему правильно" }
      ]
    }
  ]
}
```

---

# 5. Шаблон кейса

```md
## Кейс: Название проблемы

**Уровень:** L1 / L2 / L3  
**Тип:** Технический / Учётный / Финансовый / Интеграция  
**Системы:** iikoFront / iikoCloud / Плагины

### 🧩 Симптомы
- что видит клиент.

### 🔍 Диагностика
1. Шаг 1  
2. Шаг 2  
3. Шаг 3  

### 🛠 Решение
- Конкретный алгоритм в действии.

### 📎 Примечания
- ссылки на регламенты,
- связанные квизы,
- связанные кейсы.
```

---

# 📗 **2. UI_COMPONENTS.md**

````md
# UI_COMPONENTS.md
Справочник UI-компонентов SM Codex Portal

Документ описывает визуальные и структурные элементы интерфейса портала.

---

# 1. Основные компоненты

## 1.1 Card
Карточка — базовый элемент интерфейса.

```html
<article class="card">
  <h3 class="card-title">Заголовок</h3>
  <p class="card-text">Описание</p>
</article>
````

### Модификаторы

* `card--soft` — светлый фон
* `card--clickable` — кнопка-карточка

---

# 2. Гриды

## 2.1 card-grid

```html
<div class="card-grid">
  <article class="card">…</article>
  <article class="card">…</article>
</div>
```

Используется:

* в кейсах,
* в калькуляторах,
* в онбординге.

---

## 2.2 onboarding-grid

```html
<div class="card-grid onboarding-grid">
  <article class="card onboarding-card">…</article>
</div>
```

Используется для онбордингов всех отделов.

---

# 3. Подменю (Subnav)

```html
<nav class="subnav-lines" data-role="support-nav">
  <button data-target="overview">Общее</button>
  <button data-target="cases">Кейсы</button>
</nav>
```

Поведение управляется модулем раздела.

---

# 4. Секции

Секции используют паттерн:

```html
<section class="section-block" data-<module>-section="overview">
  <h2>Заголовок</h2>
  <p class="lead">Описание</p>
</section>
```

---

# 5. Модальные окна

## 5.1 Квизовый поп-ап

Создаётся глобально.

Элементы:

* `.quiz-backdrop`
* `.quiz-dialog`
* `[data-quiz-close]`

## 5.2 Калькуляторный поп-ап

Подключается в разделе «Калькуляторы».

Элементы:

* `.calc-backdrop`
* `.calc-dialog`
* `[data-calc-close]`

---

# 6. Матрицы

```html
<article class="card card--soft matrix-card">
  <h3 class="matrix-title">Матрица</h3>
  <div class="table-wrapper">
    <table>…</table>
  </div>
</article>
```

---

# 7. Sticky-паттерны

* шапка сайта фиксируется сверху,
* первая панель фильтров фиксируется под шапкой,
* вложенные панели фиксируются под родительской.

Этот UI-паттерн стандартен для всех разделов.
