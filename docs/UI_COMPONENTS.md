# UI_COMPONENTS.md
Полный справочник UI-компонентов SM Codex Portal

Документ описывает визуальные и структурные элементы интерфейса портала и правила их применения.
Цель — обеспечить единый стиль, удобство поддержки и согласованность интерфейса между всеми разделами.

---

# 1. Основные принципы UI проекта

1. **Единый визуальный стиль**
   - цвета, отступы, шрифты — единые для всех модулей.
   - карточки, таблицы, секции выглядят консистентно.

2. **Компонентный подход**
   - каждый UI-элемент считается компонентом (карточка, грид, секция, модалка).

3. **Универсальность**
   - всё, что возможно переиспользовать — оформляется как компонент.

4. **Адаптивность**
   - компоненты автоматически перестраиваются под мобильное разрешение.

5. **Минимум уникальных стилей**
   - используется готовая библиотека классов, минимизация кастомных CSS.

---

# 2. UI-компоненты

---

# 2.1. Card (Карточка)

Карточка — основной блок интерфейса.

```html
<article class="card">
  <h3 class="card-title">Заголовок</h3>
  <p class="card-text">Описание текста или инструкции.</p>
</article>

## Модификаторы

### `card--soft`

Мягкая карточка с фоновым оттенком

```html
<article class="card card--soft">…</article>
```

Использование:

* матрицы,
* дополнительные подсказки,
* подблоки разделов.

### `card--clickable`

Делает карточку интерактивной (например, для калькуляторов)

```html
<article class="card card--clickable" data-calc-open="l2">
  …
</article>
```

### `onboarding-card`

Карточка для онбординга

```html
<article class="card onboarding-card">
  <h3>1 месяц</h3>
  <ul>
    <li>Цель месяца</li>
  </ul>
</article>
```

---

# 2.2. Гриды

## `card-grid`

Универсальная сетка карточек.

```html
<div class="card-grid">
  <article class="card">...</article>
  <article class="card">...</article>
</div>
```

Использование:

* списки карточек,
* интерфейсы калькуляторов,
* вывод кейсов.

## `onboarding-grid`

Специализированная сетка для онбордингов

```html
<div class="card-grid onboarding-grid">
  <article class="card onboarding-card">...</article>
</div>
```

Особенности:

* равномерное распределение,
* мобильная перестройка → вертикальная колонка,
* фиксированная высота карточек.

---

# 2.3. Секции (`section-block`)

```html
<section class="section-block" data-support-section="overview">
  <h2>Заголовок раздела</h2>
  <p class="lead">Описание раздела</p>
</section>
```

Применение:

* логическое деление страницы,
* каждая секция соответствует пункту подменю (subnav),
* каждая секция содержит только один смысловой блок.

## Рекомендации:

* один крупный смысл = одна секция,
* избегать вложенных секций,
* заголовок обязателен.

---

# 2.4. Подменю (Subnav)

```html
<nav class="subnav-lines" data-role="support-nav">
  <button class="subnav-pill" data-target="overview">Общее</button>
  <button class="subnav-pill" data-target="cases">Кейсы</button>
  <button class="subnav-pill" data-target="matrix">Матрица</button>
</nav>
```

Характеристики:

* работает в связке с JavaScript модуля,
* переключает видимость секций,
* поддерживает sticky-паттерны.

### Правила:

* количество кнопок должно соответствовать числу секций,
* `data-target="id"` → `data-*-section="id"`.

---

# 2.5. Модальные окна

## Типы модалок:

* поп-ап квизов
* поп-ап калькуляторов

---

## Квизовый поп-ап

```html
<div class="quiz-backdrop" data-quiz-backdrop hidden>
  <div class="quiz-dialog">
    <button class="quiz-close" data-quiz-close>×</button>
    <div class="quiz-body"></div>
  </div>
</div>
```

Особенности:

* инициализируется глобально на всём проекте,
* перекрывает всю страницу,
* закрывается по фону и по кнопке.

---

## Калькуляторный поп-ап

```html
<div class="calc-backdrop" data-calc-backdrop hidden>
  <div class="calc-dialog">
    <button class="calc-close" data-calc-close>×</button>
    <div class="calc-dialog-body" data-calc-body></div>
  </div>
</div>
```

Особенности:

* использует шаблоны из `templates.js`,
* поддерживает любой калькулятор,
* одинаковый UI-паттерн для всех мотивационных расчётов.

---

# 2.6. Таблицы

Используются в матрицах компетенций и ответственности.

```html
<div class="table-wrapper">
  <table>
    <thead>
      <tr><th>Навык</th><th>L1</th><th>L2</th><th>L3</th></tr>
    </thead>
    <tbody>
      <tr><td>Понимание iiko</td><td>✓</td><td>✓</td><td>✓</td></tr>
    </tbody>
  </table>
</div>
```

Правила:

* таблица должна быть scrollable в мобильном режиме,
* ширина колонок адаптивная.

---

# 2.7. Текстовые стили

### `lead`

Крупный подзаголовок:

```html
<p class="lead">Описание раздела</p>
```

### `small`

Мелкий второстепенный текст:

```html
<p class="small">Примечание</p>
```

---

# 2.8. Иконки / Эмодзи

Проект использует эмодзи как лёгкие визуальные маркеры:

* 🔧 технические вещи
* 🧩 кейсы
* 📈 мотивация
* 💬 коммуникации
* ❗ предупреждения

Правила:

* не использовать более 1–2 эмодзи на один блок,
* не использовать анимированные или редкие символы,
* соблюдать последовательность (например, L1 всегда 🔵, L2 🟡, L3 🔴 — если будет введено).

---

# 2.9. Sticky-паттерны

Компоненты фиксируются при прокрутке:

1. Вся шапка (`header`) фиксирована сверху.
2. Первое подменю раздела (`subnav-lines`) фиксируется под шапкой.
3. Вложенные фильтры (например в кейсах) фиксируются под первым.

Правила:

* sticky работает только для верхних панелей,
* нельзя фиксировать произвольные элементы.

---

# 2.10. Кнопки

Стилевые классы:

### `btn`

Базовая кнопка.

### `btn-primary`

Кнопка основного действия.

### `btn-outline`

Прозрачная кнопка.

Правила:

* кнопки не должны содержать inline-стилей,
* текст должен быть коротким и информативным.

---

# 3. Антипаттерны (что нельзя делать)

❌ Использовать уникальные CSS-классы для разовых задач
❌ Менять padding/width через inline-стили
❌ Использовать таблицы для верстки контента
❌ Вставлять `<br>` для отступов
❌ Создавать секции без заголовков
❌ Создавать уникальные карточки, отличающиеся от остальных

---

# 4. Как расширять набор UI-компонентов

1. Новый компонент вводится **только при повторе более чем 3 раз**.

2. Компонент должен:

   * быть универсальным,
   * подходить минимум для 2 разделов,
   * быть описан в документации (в этом файле),
   * иметь консистентный CSS.

3. Нужно обновить документацию (`UI_COMPONENTS.md` и `ARCHITECTURE.md`).

---

# 5. Заключение

Этот документ описывает базовые и продвинутые UI-компоненты проекта.
Соблюдение стандартов гарантирует:

* одинаковый пользовательский опыт,
* высокую читаемость интерфейса,
* простоту добавления новых блоков,
* отсутствие визуального хаоса,
* соответствие лучшим практикам современных платформ.
