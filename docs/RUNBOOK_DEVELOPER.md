# SM Codex Portal · RUNBOOK_DEVELOPER

Практический гайд для разработчиков, ответственных за поддержку и развитие проекта.

Цель документа:
- дать быстрые пошаговые инструкции,
- объяснить архитектурные правила,
- стандартизировать работу над проектом.

---

# 1. Быстрые ссылки

- Фронтовая архитектура: `ARCHITECTURE.md`
- Гайд по расширению: `DEV_GUIDE.md`
- Навигация по документации: `INDEX.md`
- План развития: `PLAN.md`
- Правила разработки: `CONTRIBUTING.md`

---

# 2. Частые задачи разработчика (и как их выполнять)

---

## 2.1. Добавить новый раздел в портал

### Шаги:

1. Создать модуль:
```
/js/modules/<name>/main.js
```
Пример шаблона:

```js
export default {
  name: "<name>",
  async init(container) {
    container.innerHTML = `...`;
  },
  destroy() {}
};
````

2. Добавить роут:

```js
Router.register("<name>", () => import(`/sm/js/modules/${name}/main.js`));
```

3. Добавить пункт в меню `index.html`.

4. Отразить изменения в:

   * `CONTENT_GUIDE.md`
   * `ARCHITECTURE.md`
   * `CHANGELOG.md`

---

## 2.2. Добавить новый калькулятор

Калькуляторы живут **только в одном месте**.

### Шаги:

1. Создать модуль:

```
/js/calculators/<calc>.js
```

2. Добавить шаблон в:

```
/js/modules/calculators/templates.js
```

3. Добавить карточку в:

```
/js/modules/calculators/main.js
```

4. Протестировать модальный попап.

---

## 2.3. Добавить новый квиз

### Шаги:

1. Создать JSON:

```
/data/quizzes/<quiz>.json
```

2. Зарегистрировать его:

```
js/modules/quiz/registry.js
```

3. Добавить кнопку вызова:

```html
<button class="quiz-trigger" data-quiz-category="support" data-quiz-id="routing">
  Пройти квиз
</button>
```

---

## 2.4. Обновить формулы калькуляторов

### Шаги:

1. Проверить файл:

```
/js/calculators/<name>.js
```

2. Обновить:

   * ставки,
   * грейды,
   * коэффициенты,
   * формулы премий.
3. При необходимости — обновить шаблон.
4. Проверить отображение в разделе «Калькуляторы».
5. Записать изменения в `CHANGELOG.md`.

---

## 2.5. Добавить новый кейс или регламент

Кейсы хранятся внутри JS-модулей раздела.

Шаги:

1. Открыть соответствующий модуль:

   ```
   /js/modules/support/main.js
   ```
2. Добавить секцию:

   ```html
   <section data-support-section="cases">...</section>
   ```
3. Убедиться, что фильтр (`subnav-lines`) содержит соответствующий `data-target`.
4. При необходимости добавить квиз или ссылку на него.

---

# 3. Архитектурные правила

### 🔹 Нельзя:

* дублировать код калькуляторов в разделах,
* писать inline JavaScript в HTML,
* создавать модули без `destroy()`,
* вносить изменения без отражения в документации.

### 🔹 Нужно:

* использовать ES-модули,
* писать чистый, короткий, читаемый код,
* уважать роутер и структуру SPA,
* поддерживать единый UI-стиль.

---

# 4. Работа с документацией

Любые изменения должны быть отражены:

* в `CHANGELOG.md`,
* в `ARCHITECTURE.md` (если архитектура меняется),
* в `CONTENT_GUIDE.md` (если разделы обновляются),
* в `PLAN.md` (если проект развивают в новых направлениях).

---

# 5. Диагностика и отладка

1. Открыть DevTools → Console
2. Проверить ошибки импорта или модулей
3. Убедиться, что router не вызывает два модуля одновременно
4. Проверить корректность модальных окон
5. Проверить корректность шаблонов калькуляторов

---

# 6. Мини-алгоритмы для разработчиков

## Добавление новой секции на странице

1. Добавить `<section data-*-section="id">`
2. Добавить кнопку в subnav:
   `<button data-target="id">Название</button>`
3. Реализовать логику переключения (уже есть в модуле)

## Исправление калькулятора

1. Проверить шаблон в `templates.js`
2. Проверить функцию `init*` в `/js/calculators`
3. Проверить формат данных
4. Проверить округления
5. Проверить расчёт НДФЛ

## Обновление документации

1. Обновить соответствующий файл в `/docs`
2. Добавить запись в `CHANGELOG.md`

---

# 7. Как держать проект в порядке

* проверять на дубли,
* соблюдать структуру модулей,
* поддерживать документацию,
* рефакторить только при необходимости,
* держать расчёты калькуляторов унифицированными.

---

# 8. Контакты ответственных

Добавляется командой разработки.

Конец документа.
